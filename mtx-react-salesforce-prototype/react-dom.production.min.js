(function(global){
  global.ReactDOM = {
    createRoot(root){
      let current = null;
      const api = { render(element){ current = element; global.__tinyReactSetRerender(() => api.render(current)); global.__tinyReactRender(root, current); } };
      return api;
    }
  };
})(window);
